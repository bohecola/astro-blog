---
interface Props {
  src: string;
  alt: string;
  width: string | number;
  height: string | number;
}

const { src, alt, width = '20' , height = '20' } = Astro.props
---

<img
  class="not-prose inline-block align-text-bottom mx-1"
  src={src}
  alt={alt}
  width={width}
  height={height}
/>