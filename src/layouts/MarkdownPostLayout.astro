---
import BaseLayout from './BaseLayout.astro';
import type { InferEntrySchema } from 'astro:content';

interface Props {
  frontmatter: InferEntrySchema<'posts' | 'english' | 'other'>
}

const { frontmatter } = Astro.props;
---

<BaseLayout title={frontmatter.title}>
  <!-- 文章 -->
  <article>
    <slot />
  </article>
</BaseLayout>
